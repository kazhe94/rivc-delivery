export interface Menu {
    title: string;
    type: string;
}

export interface Product {
    weight: number,
    price: number,
    title: string,
    description: string,
    category: string
}
